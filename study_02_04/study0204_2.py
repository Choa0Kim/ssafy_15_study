# 비트 연산자
print(1 << 2)  # 0100(2) = 4


ATTACK = 0
JUMP = 0
MOVE = 0

STATUS = 0
STATUS = STATUS | 100 # 공격중인 상태로 변경

arr = [1, 2, 3]
n = len(arr)
subsets = []

# 1 << n : 전체 부분 집합의 수
for mask in range(1 << n):
    subsets = []
    for i in range(n):
        # if mask & (1 << i) : 해당 자리 수의 숫자가 현재 집합부분에
        if mask & ()

# & 연산자




