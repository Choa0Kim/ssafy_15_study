import sys 
sys.stdin = open("input3.txt", "r")

# T = int(input())
# for tc in (1, 1+T):
code = ""  
N, M = map(int, input().split())
# 0만 있는 행 무시하기
for i in range(N):
  
  strr= input()
  # 만약 입력받은 줄에 0만 있다면 continue
  # code라는 문자열에서 0을 제거했을때, 빈 문자열인가?
  if strr.strip('0') != "":
   # 그 줄에서 0이 아닌 부분 찾기
  #  for j in range(M):
  #    #만약 0이 아닌 위치를 찾았다면
  #    if strr[j] != '0':
  #      code += strr[j:j+15]

     
    print(strr)



  # # code에 넣기
  # code += strr +'\n' # 한 줄씩 엔터로 받아오기

#받아온 code의 양쪽 0제거하기


  



# 1. 0만 있는 행은 무시한다
# 2. 같은 행이 여러 번 나오면 하나만 사용한다
# 3. 16진수 데이터를 2진수로 변환한다
# 4. 왼쪽의 의미 없는 0은 제거한다
# 5. 바코드는 항상 1로 끝나므로 오른쪽부터 스캔한다
# 6. 숫자는 1-0-1 패턴으로 구성된다. (각각이 구간)
# 7. 세 구간 길이를 최소값으로 나누어 비율을 구한다
# 8. 비율을 이용해 숫자를 해독한다
# 9. 숫자 8개가 모이면 하나의 암호가 완성된다
# 10. 검증 코드 공식으로 올바른 암호인지 확인한다
# 11. 이미 나온 암호라면 중복 계산하지 않는다
